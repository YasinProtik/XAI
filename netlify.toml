[build]
  command = "npm run build && rm -rf public && mkdir -p public && if [ -d .output/public ]; then cp -R .output/public/. public/; elif [ -d .netlify/static ]; then cp -R .netlify/static/. public/; else echo 'No static output directory found'; ls -la; exit 1; fi"
  publish = "public"
